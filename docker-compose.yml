services:
  web:
    build:
      context: .
      args:
        NEXT_PUBLIC_SUPABASE_URL: ${NEXT_PUBLIC_SUPABASE_URL}
    ports:
      - "5000:5000"
    env_file: .env
    environment:
      PORT: 5000
      HOSTNAME: 0.0.0.0
    restart: unless-stopped
